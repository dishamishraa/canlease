// These are the default development values. 
// To support a different environment mount a new config.js using docker
window.env = { 
  REACT_APP_BFF_URL: "",
  REACT_APP_SESSION_COOKIE_NAME: "admin-frontend-session",
}
