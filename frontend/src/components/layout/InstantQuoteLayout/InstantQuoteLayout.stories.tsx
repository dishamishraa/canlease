import React from 'react';
import InstantQuoteLayout from './InstantQuoteLayout';

export default {
  title: 'layout/InstantQuoteLayout',
  component: InstantQuoteLayout,
};

export const Default: React.VFC<{}> = () => (
  <InstantQuoteLayout
    />
);