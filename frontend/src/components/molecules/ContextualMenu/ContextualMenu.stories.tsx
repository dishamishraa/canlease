import React from 'react';
import ContextualMenu from './ContextualMenu';

export default {
  title: 'molecules/ContextualMenu',
  component: ContextualMenu,
};

export const Default: React.VFC<{}> = () => (
  <ContextualMenu
    />
);
