import React from 'react';
import StartApplicationBlock from './StartApplicationBlock';

export default {
  title: 'blocks/StartApplicationBlock',
  component: StartApplicationBlock,
};

export const Default: React.VFC<{}> = () => (
  <StartApplicationBlock
    />
);
