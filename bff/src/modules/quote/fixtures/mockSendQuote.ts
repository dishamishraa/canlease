import { SendQuote } from '../types';

const mockSendQuote: SendQuote = {
  companyName: 'potato company',
  submittedBy: 'some guy (guy@potato.ca)',
  email: 'test@testmail.com',
  quoteId: 'abc',
};

export default mockSendQuote;
